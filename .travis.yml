language: c++
  

before_install:
  - pip install --user cpp-coveralls

addons:
  sonarcloud:
    organization: "edmuntremmy"
    token: $SONAR_TOKEN
      secure: 3b6c2f731006b255927abaeddc0d939bc2071e7f
script:
  - qmake
  - make
  - ./tests/tests

after_success:
  - coveralls --root . -E ".*external.*" -E ".*tests.*"
  - build-wrapper-linux-x86-64 --out-dir bw-output make clean all
  - sonar-scanner -Dsonar.cfamily.build-wrapper-output=bw-output

notifications:
  email: false
